services:
  front:
    build:
      context: .
    container_name: front-eval3
    ports:
      - "8080:8080"
    environment:
      BACKJS_URL: http://localhost:8081
      BACKPY_URL: http://localhost:8082